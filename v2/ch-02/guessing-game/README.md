### Notes:

- `Cargo.lock` ensures reproducibility of builds. Therefore, this should be included in source control builds.
- `cargo doc --open` allows you to open documentation locally
- `cargo check` allows you to quickly check if your code will compile
